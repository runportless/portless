{
  "name": "portless-store",
  "private": true,
  "engines": { "node": ">=22.12" },
  "workspaces": ["apps/checkout", "apps/orders"],
  "scripts": {
    "test": "npm run test --workspaces --if-present"
  }
}
